# Load Gazatteer from portfolio repo

# For each row in the gazettee, compile a regex pattern that matches all varients

# Only process files with filenames including dates on/after 07-20-2023

# Use nested dictionary

# Write this to a file named regex_counts.tsv
